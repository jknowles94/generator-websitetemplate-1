language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-websitetemplate' ]; then cd .. && eval "mv $currentfolder generator-websitetemplate" && cd generator-websitetemplate; fi

